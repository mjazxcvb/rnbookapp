import { styled } from "nativewind";
import { Text, TouchableOpacity, View } from "react-native";

export const StyledCloseButton = styled(
  TouchableOpacity,
  "p-3 m-auto absolute right-2 -top-10"
);
